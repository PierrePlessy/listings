.modal.fade{"aria-labelledby" => "myModalLabel", "data-is" => "chatModal", :role => "dialog", :tabindex => "-1"}
  .modal-dialog{:role => "document"}
    .modal-content
      = form_tag listing_contact_path, data: {is: 'chatForm'} do
        .modal-header
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          %h4#myModalLabel.modal-title= "Chat"
        .modal-body
          .form-group
            %label= t('your_message')
            = text_area_tag 'message', nil, class: 'form-control', data: {is: 'chat'}
        
